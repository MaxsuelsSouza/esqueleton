node_modules/
dist/
# Arquivos de ambiente (inclui os perfis .env.loja1, .env.loja2, …) — só os .example são versionados
.env
.env.*
!.env.example
!.env.*.example
.env.local
.next/
*.tsbuildinfo
.vercel/
# Grafos de código gerados pelo graphify (recriáveis com: graphify update <path>)
graphify-out/

# Ferramentas locais de analise (nao fazem parte do projeto)
ECC/
cvelistV5/
