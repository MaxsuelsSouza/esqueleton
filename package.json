{
  "name": "esqueleton-project",
  "private": true,
  "scripts": {
    "dev": "docker-compose up -d && pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
