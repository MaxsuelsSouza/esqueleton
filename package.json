{
  "name": "esqueleton-project",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r --no-bail test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
