// Validações dos dados de notificação
import { z } from 'zod'

// Atualmente não há body de entrada nas rotas de notificação — mantido para consistência com os outros módulos
export const notificationSchemas = {
  // Nenhuma validação de entrada necessária por enquanto
}
